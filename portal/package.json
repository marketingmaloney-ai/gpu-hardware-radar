{
  "name": "price-drop-radar-portal",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync:heal": "node ../backend/pipeline/self_healing_sync_engine.js && npm run build",
    "audit:versus": "node ../backend/pipeline/audit_versus_hub.js",
    "sync:versus": "node ../backend/pipeline/cron_versus_hub_self_healing.js",
    "deploy:prod": "npm run build && npm run audit:versus && npx wrangler pages deploy dist --project-name price-drop-radar --commit-dirty=true",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^4.15.0",
    "tailwindcss": "^3.4.10"
  }
}
